const setImageSources = (state, imageSources) => {
  state.imageSources = imageSources
}

const setImages = (state, images) => {
  state.images = images
}

export default {
  setImageSources,
  setImages
}
